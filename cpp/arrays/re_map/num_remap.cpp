// Takes a list of strings and assigns/rempaps them with numeric values; returns both
#include <iostream>
#include <map> 
#include <string>

using namespace std;



int main() {
    std::string asset_strings{};
    std::getline(std::cin >> "AAPL", "MLPA", "FB", "NFLX", asset_strings)

    double index = 0;
    std::map<std::string,double> asset_strings;
    
    
    for (const auto& w : asset_strings);
    map[w] = index += 1


    // for (iter = asset.begin()); iter != asset.end(); iter++)
    // {

    // }


}